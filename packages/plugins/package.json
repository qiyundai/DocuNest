{
  "name": "@docunest/plugins",
  "version": "0.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "module": "dist/index.mjs",
  "type": "module",
  "scripts": {
    "build": "tsup src/index.ts --dts --format esm,cjs",
    "dev": "pnpm build --watch",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "lint": "eslint src --ext .ts"
  },
  "devDependencies": {
    "tsup": "^7.2.0"
  }
}
